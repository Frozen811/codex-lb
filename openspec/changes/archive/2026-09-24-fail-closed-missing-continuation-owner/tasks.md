- [x] `app/modules/proxy/_service/compact.py`: remove the `len(selection_inputs.accounts) != 1` single-account heuristic. When `previous_response_preferred_account_id is None`, record `_record_continuity_fail_closed` and immediately raise `ProxyResponseError(502, openai_error("previous_response_owner_unavailable", "Previous response owner account is unavailable; retry later.", error_type="server_error"))`.
- [x] `app/modules/proxy/_service/streaming/retry.py`: remove the `len(selection_inputs.accounts) != 1` heuristic and `account_ids=None` query. When `preferred_account_id is None` for a request carrying `previous_response_id`, record `_record_continuity_fail_closed`, yield the terminal `response.failed` event with `previous_response_owner_unavailable`, write the request log, and return.
- [x] `app/modules/proxy/_service/websocket/mixin.py`: enforce fail-closed behavior on both connect-time (around line 2197) and socket reuse (around line 1928) when `previous_response_id` is present but `_resolve_websocket_previous_response_owner` returns `None`. Record continuity failure and raise/emit `previous_response_owner_unavailable` instead of falling through to unproven accounts.
- [x] `app/modules/proxy/api.py`: in `_select_responses_model_source_with_continuity`, check for known subscription continuity ownership from turn-state headers (`x-codex-turn-state` / `turn-state`) via `_resolve_compact_turn_state_owner(fail_on_missing=False)` in addition to `previous_response_id`. When a subscription owner is proven, suppress the model source.
- [x] `app/modules/proxy/_service/websocket/mixin.py`: in lines 1958 and 3708, check that neither `previous_response_owner_account_id` nor `preferred_account_id` is set before applying `responses_model_is_source_owned`, preventing spurious `model_source_requires_http_transport` rejections for active subscription sessions.
- [x] Add regression tests in `tests/integration/test_proxy_responses.py` proving that a `previous_response_id` lookup miss fails closed even when exactly one account in the pool supports the requested model.
- [x] Add regression tests in `tests/integration/test_proxy_websocket_responses.py` proving that WebSocket requests fail closed on missing `previous_response_id` owner rather than routing to an arbitrary account or reusing the socket.
- [x] Add regression tests in `tests/integration/test_model_source_routing.py` proving that known turn-state subscription owners suppress model-source routing and prevent `model_source_requires_http_transport` rejections.
- [x] Run quality gates and line count checks: `uv run ruff check app tests`, `python scripts/check_proxy_architecture.py`, `bunx @fission-ai/openspec@1.11.0 validate --specs`.
